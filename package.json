{
  "name": "math-utils",
  "version": "1.0.0",
  "description": "basic math functions and tests",
  "main": "src/sum.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --ci --reporters=default --reporters=jest-junit",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "jest-junit": {
    "outputDirectory": "./test-reports",
    "outputName": "junit.xml"
  }
}
